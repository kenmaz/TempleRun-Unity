using UnityEngine;
using System.Collections;

public class MainCamera : MonoBehaviour {
	 
	public User user;
	private Vector3 cameraUserOffset;

	// Use this for initialization
	void Start () {
		//this.cameraUserOffset = user.transform.position - this.transform.position;
	}
	
	// Update is called once per frame
	void Update () {
//		this.transform.position = user.transform.position;
//		this.transform.rotation = user.transform.rotation;
//		this.transform.position = new Vector3( 
	}
}
